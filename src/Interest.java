public interface Interest {
	
	/*
	 * Abstract method addInterest()
	 * Input:
	 *  double i: holds the rate of interest amount
	 * Process:
	 * 	waiting to be overridden by implementing class
	 * Output:
	 * 	N/A
	 */
	public void addInterest(double i);
	
}
